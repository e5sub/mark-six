services:
  mark-six:
    build: .
    container_name: mark-six
    environment:
      - TZ=Asia/Shanghai
      - FLASK_ENV=production
    volumes: 
      - ./data:/app/data
    ports:
      - "5000:5000"
    restart: unless-stopped
