services:
  mark-six:
    image: ghcr.io/e5sub/mark-six:dev
    container_name: mark-six
    environment:
      - TZ=Asia/Shanghai
      - FLASK_ENV=production
    volumes: 
      - /opt/mark-six:/app
    ports:
      - "5000:5000"
    restart: unless-stopped
